<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lzcer.interfaceplatform.accesscontrol.UserMapper">
    <resultMap id="userRecord" type="com.lzcer.interfaceplatform.accesscontrol.UserMapper$UserRecord">
        <constructor>
            <idArg column="id" javaType="long"/>
            <arg column="username" javaType="String"/>
            <arg column="password_hash" javaType="String"/>
            <arg column="display_name" javaType="String"/>
            <arg column="role" javaType="String"/>
            <arg column="enabled" javaType="boolean"/>
            <arg column="token_version" javaType="long"/>
            <arg column="last_login_at" javaType="java.time.LocalDateTime"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
            <arg column="updated_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <sql id="userColumns">
        id, username, password_hash, display_name, role, enabled, token_version,
        last_login_at, created_at, updated_at
    </sql>

    <select id="countUsers" resultType="long">select count(*) from ip_user</select>
    <insert id="insert">
        insert into ip_user(username, password_hash, display_name, role, enabled)
        values (#{username}, #{passwordHash}, #{displayName}, #{role}, #{enabled})
    </insert>
    <select id="findById" resultMap="userRecord">
        select <include refid="userColumns"/> from ip_user where id = #{id}
    </select>
    <select id="findByUsername" resultMap="userRecord">
        select <include refid="userColumns"/> from ip_user where username = #{username}
    </select>
    <select id="findAll" resultMap="userRecord">
        select <include refid="userColumns"/> from ip_user order by id
    </select>
    <update id="updateLastLogin">update ip_user set last_login_at = current_timestamp where id = #{id}</update>
    <update id="updateProfileAndInvalidateTokens">
        update ip_user set display_name = #{displayName}, role = #{role}, enabled = #{enabled},
               token_version = token_version + 1, updated_at = current_timestamp where id = #{id}
    </update>
    <update id="updatePasswordAndInvalidateTokens">
        update ip_user set password_hash = #{passwordHash}, token_version = token_version + 1,
               updated_at = current_timestamp where id = #{id}
    </update>
    <update id="invalidateTokens">update ip_user set token_version = token_version + 1 where id = #{id}</update>
    <update id="disableAndInvalidateTokens">
        update ip_user set enabled = false, token_version = token_version + 1,
               updated_at = current_timestamp where id = #{id}
    </update>
    <select id="countOtherEnabledAdministrators" resultType="long">
        select count(*) from ip_user where role = 'ADMIN' and enabled = true and id &lt;&gt; #{excludedId}
    </select>
</mapper>
