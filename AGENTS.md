# AGENTS.md

本文件是 AI 编程助手和项目开发者在本仓库内工作的统一约束。修改代码前先阅读本文件及 `docs/architecture.md`。

## 产品边界

这是企业内部的轻量接口平台，不是第一阶段就建设完整 iPaaS。

第一阶段必须支持：

1. HTTP 接口统一入口和透明转发。
2. 调用已有 SAP HTTP 接口。
3. 多数据源配置和受控的只读 SQL 查询接口。
4. 完整调用日志和 Trace ID 链路追踪。
5. 基础用户、调用方凭证和接口权限。

第一阶段暂不建设：复杂参数映射、可视化编排、消息总线、自动重试、高可用集群、完整审计中心。

## 架构规则

- 保持模块化单体 Spring Boot，不引入 Spring Cloud 微服务拆分。
- 前端位于 `frontend/`，后端位于 `server/`；开发时分离，发布时合并为一个 JAR。
- 后端 Java 包按业务能力组织，禁止把所有逻辑堆入 `controller` 或 `util`。
- 管理接口统一使用 `/api/**`，外部系统入口统一使用 `/open-api/**`，监控使用 `/actuator/**`。
- 业务转发、SQL 查询、鉴权、日志记录必须是独立服务，避免互相硬编码。
- `DataProcessor` 中的业务控制器和动态 SQL 拼接代码不得直接复制到本项目。

## 安全红线

- 外部调用方永远不能提交原始 SQL。
- SQL 只能由管理员配置，必须是单条 `SELECT` 或受支持的只读语句，并使用命名参数预编译。
- 数据源必须使用只读账号，并限制最大行数、执行超时和连接池。
- 数据库密码、AppSecret、Token 不得明文入库、写入日志或提交到 Git。
- 日志请求体和响应体必须经过字段脱敏、长度限制和内容类型判断。
- HTTP 转发必须限制目标地址，防止通过配置形成 SSRF。

## 后端约定

- Java 17，Spring Boot 4.1。
- 默认使用构造器注入。
- API 返回 `ApiResponse<T>`，异常由全局异常处理器转换。
- 数据库变更必须新增 Flyway 脚本，已发布迁移文件禁止修改。
- 所有转发请求生成 Trace ID，并贯穿入口日志、转发日志和响应头。
- 新增功能至少覆盖正常路径和关键失败路径测试。

## 前端约定

- Vue 3 Composition API、TypeScript 严格模式。
- 页面放在 `frontend/src/views`，通用组件放在 `frontend/src/components`，请求统一放在 `frontend/src/api`。
- 不在页面中散落目标服务器地址；开发代理由 `vite.config.ts` 管理。
- 保持企业管理后台风格：深色导航、浅色工作区、紧凑表格、明确状态色。
- 不用颜色作为唯一状态提示；状态必须同时显示文本或图标。
- 页面需要支持 1366px 桌面端，并在 680px 以下可基本操作。

## 修改与验证

完成代码修改后至少执行：

```powershell
.\scripts\build.ps1
```

涉及后端时运行 Maven 测试；涉及前端时运行 TypeScript 检查和生产构建。不要提交 `target/`、`node_modules/`、`dist/`、`logs/`、`data/`。

