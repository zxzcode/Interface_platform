# 一期开发路线与实际状态

状态说明：已落地 = 可以在当前源码中找到实现；待联调 = 已有部分实现但不能宣称可用；待开发 = 尚无完成实现或未定方案。

## 工程与运行时

- [x] 模块化 Spring Boot + Vue 工程，发布时合为单 JAR
- [x] HTTP 路由解析、透明转发、超时、报文上限、Trace ID 与调用日志
- [x] 数据源密文存储、动态只读连接池、SQL API 路由与命名参数执行
- [x] 系统档案、HTTP 接口、数据源、SQL API 的后端 CRUD 和在线测试端点
- [ ] Java 17 全量构建、前端生产构建、单 JAR 启动的最终验证

## 身份、凭证与授权

- [x] 后端 JWT 登录、令牌版本失效、用户 CRUD、首位管理员环境变量初始化
- [x] 后端调用方 AppKey/AppSecret 密文保存、HMAC-SHA256、时间窗、Nonce 防重放与运行时逐资源校验
- [x] HTTP 目标 URL 与目标系统 `baseUrl` 的协议/主机/端口一致性校验
- [ ] 前端登录响应字段与后端 `accessToken` 对齐
- [ ] 调用方页面与后端 `code + permissions`、嵌套 Secret 响应对齐
- [ ] 决定并实现已测试的权限编辑 API（当前前端的 `/api/clients/{id}/permissions` 尚未发布）
- [ ] 所有角色的改密和退出体验：当前 VIEWER 受后端授权规则阻断
- [ ] DNS/IP 网段白名单、登录失败锁定、限流与审计中心（不作为一期上线前置，除非安全评审要求）

## 前端与产品闭环

- [x] 登录、用户、系统、调用方、调用指南等页面代码已落地
- [ ] 管理端与真实后端响应字段逐页联调
- [ ] 1366px 与 680px 可操作性人工验收
- [ ] 从调用指南复制的 PowerShell/Java 示例真实联调

## 试运行与上线

- [ ] WMS → 平台 → SAP 的真实 HTTP 接口联调
- [ ] 至少一个真实多数据源只读 SQL API 联调
- [ ] 鉴权失败、超时、目标失败、SQL 参数失败的日志追踪复核
- [ ] 发布回滚、备份恢复、运维监控与压力测试

一期明确不包括可视化编排、MQ/ESB、自动重试补偿、高可用集群和 Spring Cloud 拆分。
